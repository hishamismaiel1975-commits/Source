info:
  name: UserInfo
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost/identity/api/v1/Auth/user/info
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
